# ===========================================
# LINE Bot Configuration
# Get these from LINE Developers Console
# https://developers.line.biz/console/
# ===========================================
LINE_CHANNEL_ACCESS_TOKEN=your_channel_access_token_here
LINE_CHANNEL_SECRET=your_channel_secret_here

# ===========================================
# Google Cloud Configuration
# Create a project at https://console.cloud.google.com/
# ===========================================
GOOGLE_PROJECT_ID=your-gcp-project-id
GOOGLE_LOCATION=us
GOOGLE_PROCESSOR_ID=your-document-ai-processor-id

# ===========================================
# Google Gemini AI (for invoice parsing)
# Get API key from: https://aistudio.google.com/app/apikey
# ===========================================
GOOGLE_GEMINI_API_KEY=your-gemini-api-key

# Google Service Account Key (Base64 encoded)
# 1. Create a service account in GCP Console
# 2. Generate a JSON key
# 3. Base64 encode it: base64 -i service-account.json | tr -d '\n'
# 4. Paste the result here
GOOGLE_SERVICE_ACCOUNT_KEY=your_base64_encoded_service_account_json

# ===========================================
# Google Drive Configuration
# ===========================================
# The folder ID where images will be stored
# Get it from the folder URL: https://drive.google.com/drive/folders/FOLDER_ID
# Share this folder with your service account email
GOOGLE_DRIVE_FOLDER_ID=your_drive_folder_id

# ===========================================
# Google Sheets Configuration
# ===========================================
# The spreadsheet ID for logging extracted data
# Get it from the sheet URL: https://docs.google.com/spreadsheets/d/SHEET_ID/edit
# Share this sheet with your service account email (Editor access)
GOOGLE_SHEET_ID=your_sheet_id
GOOGLE_SHEET_NAME=Sheet1

# ===========================================
# Server Configuration
# ===========================================
PORT=3000
NODE_ENV=development

# ===========================================
# Google Drive - OAuth 2.0 (for file uploads)
# ===========================================
# Create OAuth credentials at: https://console.cloud.google.com/apis/credentials
# Then run: node scripts/get-oauth-token.js to get the refresh token
GOOGLE_OAUTH_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_OAUTH_CLIENT_SECRET=GOCSPX-your-client-secret
GOOGLE_OAUTH_REFRESH_TOKEN=your-refresh-token-from-script

# ===========================================
# OCR Quota Configuration
# ===========================================
# Maximum OCR requests per month (default: 975 to stay under 1000 free tier)
OCR_MONTHLY_LIMIT=975

# Message shown when quota is exceeded
OCR_QUOTA_MESSAGE=You are out of OCR quota, please contact admin.

# ===========================================
# Bot Behavior
# ===========================================
# Set to 'true' to send step-by-step processing updates via LINE
# Set to 'false' (or omit) for minimal messages
VERBOSE=true
